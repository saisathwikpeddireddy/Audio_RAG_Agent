# Audio RAG Auto-Editor dependencies.
# Intentionally lightweight: NO torch / tensorflow / local models.
# All heavy compute is offloaded to cloud APIs (Groq, Pinecone, Gemini).

pinecone>=6.0.0          # Serverless vector DB + integrated (hosted) embeddings
groq>=0.11.0             # Whisper-large-v3-turbo transcription
google-generativeai>=0.8.0  # Gemini 2.0 Flash "editor" logic
pydub>=0.25.1            # Millisecond MP3 slicing + crossfades (needs ffmpeg)
python-dotenv>=1.0.0     # Loads API keys from .env

# Optional: only needed if you switch EMBED_PROVIDER=openai in .env
# openai>=1.40.0
